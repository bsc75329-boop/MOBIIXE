# 깃허브 초보자를 위한 사용 가이드

이 문서는 깃허브(GitHub)를 처음 사용하는 분을 위해 MOBIIXE 프로젝트의 코드를 다운로드하고 아두이노에 업로드하는 전체 과정을 설명합니다.

---

## 목차

1. [필요한 프로그램 설치](#1-필요한-프로그램-설치)
2. [깃허브에서 코드 다운로드하기](#2-깃허브에서-코드-다운로드하기)
3. [아두이노 IDE에서 코드 열기](#3-아두이노-ide에서-코드-열기)
4. [아두이노에 업로드하기](#4-아두이노에-업로드하기)
5. [모터 제어하기](#5-모터-제어하기)
6. [코드 수정 후 깃허브에 올리기 (선택)](#6-코드-수정-후-깃허브에-올리기-선택)

---

## 1. 필요한 프로그램 설치

### Arduino IDE 설치

1. https://www.arduino.cc/en/software 에 접속
2. 본인 운영체제(Windows/Mac/Linux)에 맞는 버전 다운로드
3. 설치 파일 실행 → 기본 설정 그대로 "다음" 클릭하여 설치 완료

### Git 설치 (선택사항 - 방법 B를 사용할 경우)

1. https://git-scm.com/downloads 에 접속
2. 본인 운영체제에 맞는 버전 다운로드
3. 설치 파일 실행 → 기본 설정 그대로 "다음" 클릭하여 설치 완료

---

## 2. 깃허브에서 코드 다운로드하기

두 가지 방법이 있습니다. **방법 A가 가장 쉽습니다.**

### 방법 A: ZIP 파일로 다운로드 (가장 쉬움)

```
① 웹 브라우저에서 깃허브 저장소 페이지에 접속

② 초록색 "<> Code" 버튼 클릭

   ┌─────────────────────────┐
   │  <> Code ▼              │
   ├─────────────────────────┤
   │  HTTPS  SSH  GitHub CLI │
   │                         │
   │  ┌───────────────────┐  │
   │  │ https://github...  │  │
   │  └───────────────────┘  │
   │                         │
   │  ┌───────────────────┐  │
   │  │ Download ZIP      │ ◄── 이것을 클릭!
   │  └───────────────────┘  │
   └─────────────────────────┘

③ 다운로드된 ZIP 파일을 원하는 폴더에 압축 해제
```

### 방법 B: Git 명령어로 다운로드 (Git 설치 필요)

```bash
# 원하는 폴더에서 터미널(명령 프롬프트)을 열고 입력:
git clone https://github.com/bsc75329-boop/MOBIIXE.git
```

이렇게 하면 `MOBIIXE` 폴더가 자동으로 생성됩니다.

> **git clone이란?** 깃허브에 있는 코드를 내 컴퓨터로 복사하는 명령어입니다.

---

## 3. 아두이노 IDE에서 코드 열기

```
① Arduino IDE 실행

② 메뉴에서: 파일 → 열기 (또는 Ctrl+O)

③ 다운로드한 폴더에서 아래 파일을 찾아 선택:
   MOBIIXE/motor_control/motor_control.ino

④ 코드가 편집 창에 표시되면 성공!
```

---

## 4. 아두이노에 업로드하기

### 4-1. 아두이노 보드 연결

```
① USB 케이블로 Arduino Uno를 컴퓨터에 연결

② Arduino IDE 메뉴에서 보드 설정:

   도구 → 보드 → Arduino AVR Boards → Arduino Uno

   ┌─────────────────────────────────┐
   │ 도구                            │
   ├─────────────────────────────────┤
   │ 보드: "Arduino Uno"        ◄── 확인 │
   │ 포트: "COM3 (Arduino Uno)" ◄── 선택 │
   └─────────────────────────────────┘

③ 도구 → 포트 에서 Arduino가 연결된 포트 선택
   - Windows: COM3, COM4 등으로 표시됨
   - Mac: /dev/cu.usbmodem... 으로 표시됨
```

> **포트가 안 보이면?** USB 케이블을 다시 연결하거나, 아두이노 드라이버를 설치하세요.

### 4-2. 코드 업로드

```
① "확인" 버튼(체크 아이콘 ✓)을 눌러 컴파일 확인
   → 하단에 "컴파일 완료" 메시지가 나오면 정상

② "업로드" 버튼(화살표 아이콘 →)을 눌러 업로드
   → 하단에 "업로드 완료" 메시지가 나오면 성공!

   ┌──────────────────────────────────────┐
   │  [✓ 확인]  [→ 업로드]                │
   │                                      │
   │  // 코드가 여기에 표시됩니다...        │
   │                                      │
   ├──────────────────────────────────────┤
   │  "업로드 완료" ◄── 이 메시지가 나오면 OK │
   └──────────────────────────────────────┘
```

---

## 5. 모터 제어하기

### 5-1. 시리얼 모니터 열기

```
① Arduino IDE 메뉴: 도구 → 시리얼 모니터 (또는 Ctrl+Shift+M)

② 우측 하단에서 통신 속도를 "9600 보드레이트"로 설정

   ┌──────────────────────────────────────────┐
   │ 시리얼 모니터                              │
   ├──────────────────────────────────────────┤
   │ === MOBIIXE 모터 제어 시스템 ===            │
   │ 명령어 안내:                                │
   │   W - 전진                                 │
   │   S - 후진                                 │
   │   A - 좌회전                               │
   │   D - 우회전                               │
   │   X - 정지                                 │
   │   1~9 - 속도 조절                           │
   ├──────────────────────────────────────────┤
   │ [입력창: 여기에 명령어 입력] [전송]    9600 ▼ │
   └──────────────────────────────────────────┘
```

### 5-2. 명령어 입력

```
상단 입력창에 글자를 입력하고 "전송" 버튼 또는 Enter 키를 누르세요:

  W + Enter → 전진
  S + Enter → 후진
  A + Enter → 좌회전
  D + Enter → 우회전
  X + Enter → 정지
  5 + Enter → 중간 속도로 변경
```

---

## 6. 코드 수정 후 깃허브에 올리기 (선택)

코드를 수정한 뒤 깃허브에 다시 올리고 싶다면 아래 단계를 따르세요.

### 6-1. 깃허브 계정 만들기

1. https://github.com 에 접속
2. "Sign up" 클릭
3. 이메일, 비밀번호, 사용자명 입력하여 가입

### 6-2. 저장소 Fork하기 (내 계정으로 복사)

```
① 원본 저장소 페이지에서 우측 상단 "Fork" 버튼 클릭

   ┌──────────────────────────────────┐
   │  bsc75329-boop/MOBIIXE          │
   │                    [⭐ Star] [🔱 Fork] ◄── 클릭!
   └──────────────────────────────────┘

② "Create fork" 클릭
   → 내 계정에 MOBIIXE 저장소가 복사됩니다
```

### 6-3. 내 컴퓨터에서 수정하고 올리기

```bash
# ① 내 Fork 저장소를 다운로드
git clone https://github.com/내아이디/MOBIIXE.git
cd MOBIIXE

# ② 코드를 수정한 뒤, 변경 사항 확인
git status

# ③ 변경된 파일을 스테이징 (업로드 준비)
git add motor_control/motor_control.ino

# ④ 커밋 (변경 이력 저장)
git commit -m "모터 속도 기본값 변경"

# ⑤ 깃허브에 업로드
git push
```

### 용어 정리

| 용어 | 뜻 |
|------|-----|
| **Repository (저장소)** | 코드를 보관하는 프로젝트 폴더 |
| **Clone (클론)** | 깃허브의 코드를 내 컴퓨터로 복사 |
| **Fork (포크)** | 다른 사람의 저장소를 내 계정으로 복사 |
| **Commit (커밋)** | 변경 사항을 기록으로 저장 |
| **Push (푸시)** | 내 컴퓨터의 커밋을 깃허브에 업로드 |
| **Pull (풀)** | 깃허브의 최신 코드를 내 컴퓨터로 가져오기 |
| **Branch (브랜치)** | 독립적인 작업 공간 (원본에 영향 없이 수정 가능) |

---

## 문제가 생겼을 때

| 증상 | 해결 방법 |
|------|----------|
| 포트가 목록에 안 보임 | USB 케이블 재연결, 다른 포트 시도, 드라이버 설치 |
| 컴파일 에러 발생 | 보드가 "Arduino Uno"로 선택되어 있는지 확인 |
| 업로드 실패 | 올바른 포트가 선택되어 있는지 확인 |
| 모터가 안 돌아감 | 배선 재확인, 외부 전원 연결 확인, GND 공통 접지 확인 |
| 시리얼 모니터에 글자 깨짐 | 통신 속도를 9600으로 설정했는지 확인 |
